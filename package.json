{
  "name": "playwright-tut",
  "version": "1.0.0",
  "description": "Generate **visual, step-by-step PDF tutorials** for web apps — \"how to do X on\r this website\" as a series of annotated screenshots with the relevant button\r **circled** on each page. Each tutorial is a **replayable Playwright TypeScript\r spec**, so the exact flow can also be re-run with `npx playwright test`.",
  "main": "tutorial_generator.ts",
  "scripts": {
    "render": "tsx tutorial_generator.ts",
    "check": "tsx tutorial_generator.ts",
    "test": "playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "pdf-lib": "^1.17.1",
    "tsx": "^4.22.4"
  }
}
